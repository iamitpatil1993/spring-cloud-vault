spring.application.name=spring-cloud-vault
spring.cloud.vault.enabled=true
spring.cloud.vault.host=localhost
spring.cloud.vault.port=8200
spring.cloud.vault.kv.enabled=true
spring.cloud.vault.kv.backend=kv
spring.cloud.vault.authentication=token
spring.cloud.vault.token=s.NKfTgBoU7sDQm7oSFrggpl94
spring.cloud.vault.scheme=http



# Defaults to: spring.application.name
# Key-Value Backend configuration
# Refer https://cloud.spring.io/spring-cloud-vault/reference/html/#vault.config.backends.kv.versioned
# to check how spring cloud builds the KV context to get key-value pait secrets
# If below property is not specified, then fallback to spring.application.name.
# If we want to use different base for KV secret context then we  can customize using below property
spring.cloud.vault.kv.application-name=spring-cloud-vault